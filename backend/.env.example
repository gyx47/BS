# Flask 密钥配置（生产环境必须修改）
SECRET_KEY=your-secret-key-please-change-in-production
JWT_SECRET_KEY=your-jwt-secret-key-please-change-in-production

# 数据库配置
# SQLite (默认)
DATABASE_URL=sqlite:///imagedb.sqlite

# MySQL (生产环境推荐)
# DATABASE_URL=mysql://username:password@localhost/imagedb

# 上传配置
MAX_CONTENT_LENGTH=16777216  # 16MB

# CORS 配置（允许的前端域名）
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
